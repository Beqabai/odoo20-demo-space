# ტექნიკური დავალება (შესწორებული რედაქცია, v2)

## მოდული: BPMN Process Management (`bpmn_process_management`)

> ეს რედაქცია ცვლის საწყის დოკუმენტს `bpmn_process_management_module_technical_specification.md`.
> პირველ ნაწილში ჩამოთვლილია, რა შეიცვალა და რატომ; მეორე ნაწილი არის მოქმედი ტექნიკური დავალება,
> რომლის მიხედვითაც მოდული აიწყო.

---

## ნაწილი I. საწყის დავალებაში აღმოჩენილი ხარვეზები და გადაწყვეტილებები

| # | საწყის დავალებაში | პრობლემა | გადაწყვეტილება |
|---|---|---|---|
| 1 | თავსებადობა: Odoo 16, 17, 18, 19, 20 | ერთი კოდით ვერ იმუშავებს: 17-ში `attrs` გაუქმდა, 18-ში `tree` → `list`, 19-ში `res.groups` → `privilege_id`, 20-ში OWL 3, ახალი უფლებების მოდელი `ir.access` (`ir.model.access` და `ir.rule` აღარ არსებობს) და Binary ველების `BinaryValue` ტიპი. | სამიზნე ვერსიები: **Odoo 19 და 20** (Community და Enterprise). ვერსიის ფრაგმენტები ავტომატურად ირჩევა გაშვებისას (იხ. II.9). |
| 2 | ორმხრივი თავსებადობა draw.io-სთან BPMN 2.0 XML-ით | draw.io-ს BPMN 2.0 XML-ის იმპორტი/ექსპორტი **არ აქვს** — ის ინახავს საკუთარ mxGraph ფორმატს. | ჩაშენებული გადამყვანები: draw.io → BPMN (იმპორტი) და BPMN → .drawio (ექსპორტი). |
| 3 | Visio არ იყო მოთხოვნებში | შეკვეთის დამატებითი პირობა: Visio-ში დახატულის მარტივი შეტანა. | ჩაშენებული გადამყვანი Visio .vsdx → BPMN. ძველი ორობითი .vsd არ არის მხარდაჭერილი (ფორმატი დახურულია) — Visio-ში უნდა შეინახოს .vsdx-ად. |
| 4 | Swimlane → `hr.department`, Documentation → `knowledge.article` / `documents.document` | `hr` არ არის დამოკიდებულებებში (`base, web, mail`), `knowledge` და `documents` მხოლოდ Enterprise-შია. პირდაპირი Many2one ამ მოდელებზე მოდულს Community-ზე ჩააგდებდა. | „რბილი“ ბმულები: მოდელის სახელი + ID + XML ID ინახება დიაგრამაში. არჩევანში ჩანს მხოლოდ დაინსტალირებული მოდელები. |
| 5 | ბმული ინახება ID-ით | ID სხვადასხვა ბაზაში განსხვავდება; ექსპორტ-იმპორტის შემდეგ ბმული სხვა ჩანაწერზე მიუთითებს. | ინახება XML ID-ც (`mail.menu_root_discuss`) და ის პრიორიტეტულია; გაწყვეტილი ბმული დიაგრამაზე ინიშნება. |
| 6 | ექსპორტი: `/web/content/?model=...&field=bpmn_xml` | `/web/content` Text ველს base64-ად ცდილობს წაკითხვას — ფაილი ან დაზიანდება, ან შეცდომა ამოვა; ქართული სახელი ფაილში არ იყო დაცული. | საკუთარი კონტროლერი `/bpmn_process_management/export/<id>/<bpmn|drawio>` წვდომის შემოწმებით და სწორი `Content-Disposition`-ით. |
| 7 | Viewer „ვერ ხედავს“ Modeler-ს | ეს მხოლოდ ინტერფეისის შეზღუდვაა; RPC-ით ჩაწერა მაინც შესაძლებელი იქნებოდა. | სერვერული დაცვა: Viewer-ს აქვს მხოლოდ წაკითხვის უფლება და ხედავს მხოლოდ გამოქვეყნებულს; შინაარსის ველების შეცვლა ნებადართულია მხოლოდ დრაფტში. |
| 8 | ვერსიონირება — მხოლოდ Char ველი `version` | ისტორია არ ინახება, ძველ ვერსიას ვერ ნახავ. | ყოველი ვერსია ცალკე ჩანაწერია (`root_id`, `previous_version_id`); ახლის გამოქვეყნებისას წინა ავტომატურად „გაუქმებული“ ხდება. |
| 9 | ქართული ტექსტები პირდაპირ კოდში | სხვა ენაზე თარგმნა შეუძლებელია, ეწინააღმდეგება Odoo-ს სტანდარტს. | კოდი ინგლისურად, სრული ქართული თარგმანი `i18n/ka.po`-ში (bpmn-js-ის პალიტრისა და მენიუების ჩათვლით). |
| 10 | იმპორტირებული ნახაზის გადამოწმება არ იყო | გარე ფაილში Odoo-ს კავშირები არ არსებობს — ვინმემ უნდა მიუთითოს, რომელი ზოლი რომელ დეპარტამენტს ეკუთვნის. | „გადასახედი“ ნიშნები (`odoo:review`), ზოლების ავტომატური შესაბამისობა სახელით და ცალკე მენიუ „გადასახედი რიგი“. |
| 11 | XML-ის უსაფრთხოება არ იყო განხილული | ატვირთული XML შეიძლება შეიცავდეს XXE / „billion laughs“ შეტევას. | უსაფრთხო პარსერი (entity-ების, DTD-ისა და ქსელის გარეშე), ზომის ლიმიტები, zip-bomb-ის შემოწმება, URL-ების თეთრი სია (`javascript:` აკრძალულია). |
| 12 | ტერმინი „სოლი (Lane)“ | ქართულად lane არის **ზოლი**; „სოლი“ სხვა მნიშვნელობისაა. | ინტერფეისსა და დოკუმენტაციაში: ზოლი (Lane), აუზი (Pool). |
| 13 | bpmn-js — `static/lib/bpmn/*.min.js` ორი ცალკე ფაილით | ორივე ბიბლიოთეკა ერთსა და იმავე გლობალურ `BpmnJS` ცვლადს წერს — ერთად ჩატვირთვისას ერთმანეთს გადაწერენ; ~1.5 მბ ყველა გვერდზე იტვირთებოდა. | ერთი ბანდლი (`odoo-bpmn.min.js`, 580 კბ: Modeler + NavigatedViewer + auto-layout) და ზარმაცი ჩატვირთვა მხოლოდ დიაგრამის გახსნისას. |
| 14 | ლიცენზია არ იყო ნახსენები | bpmn-js-ის ლიცენზია მოითხოვს, რომ „bpmn.io“ ლოგო დიაგრამაზე ჩანდეს. | ლოგო არ იმალება; ლიცენზია ფაილშია `static/lib/bpmn-js/LICENSE-bpmn-js`. |

---

## ნაწილი II. მოქმედი ტექნიკური დავალება

### 1. მიზანი და ფილოსოფია

მოდული არის ორგანიზაციის პროცესების **საცავი, მოდელირების ინსტრუმენტი და ცოდნის ბაზა** (Single Source of Truth).
ის **არ არის** Workflow Execution Engine: არ ბლოკავს ღილაკებს, არ ცვლის სხვა დოკუმენტების სტატუსებს და არ ერევა
გაყიდვების, შესყიდვების, საწყობისა და ბუღალტერიის ლოგიკაში.

### 2. თავსებადობა

- Odoo **19.0** და **20.0**, Community და Enterprise; ერთი საკოდო ბაზა.
- დამოკიდებულებები: მხოლოდ `base`, `web`, `mail`.
- არჩევითი ინტეგრაცია (თუ დაინსტალირებულია): `hr` (დეპარტამენტი, თანამდებობა), `knowledge` და `documents` (Enterprise).
- გარე ფორმატები: BPMN 2.0 XML (Camunda Modeler, Signavio, bpmn.io, Bizagi და სხვ.), draw.io, Microsoft Visio (.vsdx).

### 3. როლები და წვდომა

| როლი | ვინ | უფლებები |
|---|---|---|
| დამთვალიერებელი (`group_process_user`) | ყველა შიდა მომხმარებელი (ავტომატურად `base.group_user`-იდან) | ხედავს მხოლოდ **გამოქვეყნებულ** პროცესებს, კითხულობს ინსტრუქციებს, ხსნის ბმულებს. ჩაწერის უფლება არ აქვს. |
| პროცესების მენეჯერი (`group_process_manager`) | ანალიტიკოსი, ხარისხის მენეჯერი | ქმნის, არედაქტირებს (მხოლოდ დრაფტს), აიმპორტებს, გზავნის განხილვაზე, აქვეყნებს, ქმნის ახალ ვერსიას. |

დამატებით: მულტიკომპანიური წესი (`company_id` ცარიელი = საერთო ყველა კომპანიისთვის).

### 4. მონაცემთა მოდელი

**`business.process`** — პროცესის ერთი ვერსია:
`name`, `code`, `version`, `state` (draft → review → published → deprecated), `category` (ძირითადი/დამხმარე/მმართველობითი),
`parent_id` (პროცესების იერარქია), `owner_id`, `company_id`, `description` (HTML), `bpmn_xml` (Text, BPMN 2.0),
`diagram_preview` (SVG მინიატურა), `root_id` / `previous_version_id` (ვერსიების ჯაჭვი), `published_date` / `published_by_id`, chatter.

**`business.process.element`** — დიაგრამის ელემენტების საძიებო ინდექსი. ყოველი შენახვისას თავიდან იგება XML-იდან
(XML რჩება ერთადერთ წყაროდ): ტიპი, სახელი, ზოლი, აუზი, პასუხისმგებელი, ბმულების რაოდენობა, გადახედვის სტატუსი და მიზეზი.
იძლევა ძიებას „რომელ პროცესებში მონაწილეობს საწყობის დეპარტამენტი?“ და კვებავს „გადასახედ რიგს“.

**`business.process.import`** — იმპორტის ოსტატი (TransientModel).

### 5. სასიცოცხლო ციკლი და ვერსიები

1. **დრაფტი** — რედაქტირდება დიაგრამა, სახელი, კოდი, აღწერა.
2. **განხილვაში** — შინაარსი იკეტება; შეიძლება დრაფტში დაბრუნება.
3. **გამოქვეყნებული** — ხილულია ყველასთვის; ამავე ჯაჭვის წინა გამოქვეყნებული ვერსია ხდება „გაუქმებული“; chatter-ში იწერება ჩანაწერი.
4. **ახალი ვერსია** (1.0 → 1.1) / **ახალი ძირითადი ვერსია** (1.x → 2.0) — იქმნება გამოქვეყნებულის ასლი დრაფტად.
   ერთდროულად მხოლოდ ერთი ვერსია შეიძლება მზადდებოდეს.

შეზღუდვა სერვერზეა (არა მარტო ინტერფეისში): დრაფტის გარდა სხვა სტატუსში `name`, `code`, `bpmn_xml`, `description`, `category` არ იცვლება.

### 6. Odoo-სთან მსუბუქი კავშირები (Soft Linkage)

მეტამონაცემები ინახება BPMN-ის სტანდარტულ `<bpmn:extensionElements>` ტეგში, საკუთარ namespace-ში
`http://gecbusiness.com/schema/bpmn/odoo/1.0` (პრეფიქსი `odoo`). გარე პროგრამები მათ უცვლელად ტოვებენ.

| ელემენტი | Odoo-ს ობიექტი | შენახვა |
|---|---|---|
| ზოლი / აუზი | `hr.department`, `hr.job`, `res.groups`, `res.users` | `<odoo:responsible model="…" resId="…" xmlid="…" name="…"/>` |
| ნებისმიერი ფიგურა | Odoo-ს მენიუ (`ir.ui.menu`), ფანჯრის მოქმედება, ჩანაწერი (`knowledge.article`, `documents.document`, `business.process`, `ir.attachment`), URL | `<odoo:link linkType="menu|action|record|url" …/>` (რამდენიმეც შეიძლება) |
| სამუშაო ინსტრუქცია | — | სტანდარტული `<bpmn:documentation>` |
| გადასახედი ნიშანი | — | `<odoo:review status="pending" reason="…" source="drawio|visio|import"/>` |

ბმულის გახსნისას სერვერი ამოწმებს: ჩანაწერი არსებობს, მომხმარებელს აქვს წვდომა, მენიუ მისთვის ხილულია, URL იწყება `https://`, `http://`, `mailto:` ან `/`-ით.

### 7. ინტერფეისი

- ველის ვიჯეტი `bpmn_editor` (Text ველზე). ორი რეჟიმი:
  - **დამთვალიერებელი** — `NavigatedViewer` (გადიდება/გადაადგილება), პალიტრის გარეშე. ფიგურაზე დაწკაპუნებით მარჯვნივ ჩანს ბარათი: ტიპი, ზოლი და პასუხისმგებელი, სამუშაო ინსტრუქცია, ბმულის ღილაკები.
  - **რედაქტორი** — `Modeler`, როცა მომხმარებელი მენეჯერია და ვერსია დრაფტშია. მარჯვენა პანელში: სახელი, ინსტრუქცია, პასუხისმგებელი (ზოლისთვის), ბმულების დამატება ძიებით, „გადახედულად მონიშვნა“.
- გაფართოებული პალიტრა: სტანდარტულ ფიგურებს ემატება ხშირად გამოყენებადები (მომხმარებლის/ხელით/სერვისის/გაგზავნის/მიღების/ბიზნესწესის ამოცანა, გამოძახების აქტივობა, აკეცილი ქვეპროცესი, პარალელური/ჩამრთველი/მოვლენაზე დაფუძნებული შლუზი, შეტყობინების/ტაიმერის მოვლენები, შეცდომის/შეწყვეტის დასასრული, ტექსტური შენიშვნა). დანარჩენი BPMN 2.0 ელემენტები — 🔧 „ელემენტის შეცვლა“ მენიუდან. Choreography/Conversation დიაგრამები bpmn-js-ში არ არის.
- ენა: ინტერფეისი (bpmn-js-ის პალიტრისა და მენიუების ჩათვლით) მიჰყვება მომხმარებლის ენას (*My Preferences → Language*); ცალკე პარამეტრი საჭირო არ არის.
- ხელსაწყოების ზოლი: გადიდება, დაპატარავება, ეკრანზე მორგება, დაბრუნება/გამეორება, ჩამოტვირთვა (BPMN, draw.io, SVG, PNG), სრული ეკრანი, „გადასახედია: N“.
- დიაგრამაზე ნიშნები: 🔗 ბმულის მქონე ფიგურა, „!“ გადასახედი ელემენტი (მხოლოდ მენეჯერს).
- „პროცესების რუკა“ — kanban დიაგრამის მინიატურებით, დაჯგუფებული ტიპით.
- „გადასახედი რიგი“ და „პასუხისმგებლობები“ — ელემენტების სია ყველა პროცესიდან.
- ვიჯეტი ტექნიკურად დამოუკიდებელია OWL-ის ვერსიისგან (OWL 2 Odoo 19-ში, OWL 3 Odoo 20-ში): OWL ფენა მხოლოდ DOM ელემენტს აწვდის ჩვეულებრივ JS კლასს.

### 8. იმპორტი და ექსპორტი

**იმპორტი** (მენიუ „დიაგრამის იმპორტი“ ან ღილაკი დრაფტის ფორმაზე):

| ფორმატი | რას ცნობს |
|---|---|
| `.bpmn` / `.xml` (BPMN 2.0) | ყველაფერს; `isExecutable` ყოველთვის `false`; თუ ფაილს განლაგება (DI) არ აქვს, გახსნისას ავტომატურად განლაგდება |
| draw.io: `.drawio`, `.xml`, `.svg`, `.png` (დიაგრამის ჩართვით), შეკუმშული და მრავალგვერდიანი | BPMN 2.0 ბიბლიოთეკის ყველა ფიგურა (ამოცანები მარკერებით, ციკლი/მრავალეგზემპლარიანი/კომპენსაცია, მოვლენები განმარტებებით, შლუზები, ქვეპროცესები, Ad-hoc, მონაცემები, ჯგუფი, შენიშვნები), swimlane-ები, cross-functional flowchart, ჩვეულებრივი flowchart ფიგურები (მართკუთხედი → ამოცანა, რომბი → შლუზი, ელიფსი → მოვლენა) |
| Visio `.vsdx` | BPMN შაბლონის ფიგურები shape data-ს მიხედვით (BpmnTaskType, BpmnEventType, BpmnTriggerOrResult, BpmnGatewayType), Cross-Functional Flowchart ზოლები, Basic Flowchart, უბრალო ნახაზები (გეომეტრიით), მიმაგრებული და მიუმაგრებელი შემაერთებლები |

გარდაქმნის წესები: sequence flow აუზებს შორის → message flow; საწყისი/დასასრულის მოვლენა ირკვევა კავშირებით;
ამოცანის საზღვარზე მოვლენა → სასაზღვრო მოვლენა; გვერდიგვერდ მდგომი ზოლები → ერთი აუზი; ელემენტების ზომები BPMN-ის პროპორციებზე.

იმპორტის შემდეგ: ყველა ზოლი ინიშნება გადასახედად; თუ დეპარტამენტი/თანამდებობა/ჯგუფი ზუსტად იმავე სახელით არსებობს,
ის ავტომატურად მიებმება, ნიშანი კი რჩება „დაადასტურეთ“; ვარაუდით განსაზღვრული ფიგურებიც ინიშნება.
ანგარიში (რა გამოტოვდა და რატომ) იწერება chatter-ში.

**ექსპორტი:** `.bpmn` (ვალიდური OMG BPMN 2.0, იხსნება Camunda Modeler-სა და Signavio-ში), `.drawio` (draw.io-ს BPMN ფიგურებით,
ინსტრუქცია — tooltip-ად, ბმულები — Edit Data-ში), SVG და PNG.

### 9. ვერსიის ავტომატური ადაპტაცია

| საკითხი | Odoo 19 | Odoo 20 | როგორ მოგვარდა |
|---|---|---|---|
| წვდომის უფლებები | `ir.model.access` + `ir.rule` | `ir.access` | `security.xml`-ში `<function>` ტვირთავს შესაბამის ფაილს (`security/odoo19/…` ან `security/odoo20/ir.access.csv`) ინსტალაციისა და ყოველი განახლებისას |
| OWL | 2 | 3 (`static props`, `useState`, `t-ref` შეიცვალა) | ვიჯეტი მათ არ იყენებს |
| Binary ველი | base64 | `BinaryValue` | `binary_content()` დამხმარე ფუნქცია |
| ხატულები | Font Awesome | Material Symbols | საკუთარი SVG ხატულები |
| `content_disposition` | `odoo.http` | `odoo.http.stream` | try/except იმპორტი |
| მოდულის ვერსია | — | — | `1.0.0` (Odoo თავად ამატებს სერიას) |

### 10. უსაფრთხოება

უსაფრთხო XML პარსერი, ფაილის ზომა ≤ 25 მბ, Visio-ს არქივი ≤ 200 მბ გაშლილი, ≤ 20 000 ფიგურა გვერდზე, URL-ების თეთრი სია,
ყველა ტექსტი პანელში ჩაისმება როგორც ტექსტი (არა HTML), ექსპორტის კონტროლერი ამოწმებს წაკითხვის უფლებას.

### 11. მიღება-ჩაბარების კრიტერიუმები (Definition of Done)

| # | კრიტერიუმი | სტატუსი |
|---|---|---|
| 1 | Viewer ხედავს მხოლოდ გამოქვეყნებულს და ვერ წერს (სერვერზეც) | ✔ ავტომატური ტესტი (19, 20) |
| 2 | draw.io-ს ფაილი იმპორტირდება; ექსპორტი იხსნება draw.io-ში | ✔ draw.io-ს ოფიციალური შაბლონები; ექსპორტი შემოწმებულია draw.io viewer-ით |
| 3 | Visio .vsdx იმპორტირდება | ✔ სინთეზური BPMN შაბლონი + რეალური .vsdx ფაილები; ⚠ საჭიროა თქვენი რეალური Visio ფაილებით გადამოწმება |
| 4 | Community & Enterprise, Odoo 19 და 20 | ✔ Community 19/20 — ინსტალაცია, განახლება, 35 ტესტი; ⚠ Enterprise ბაზაზე არ გაშვებულა (წვდომა არ იყო), Enterprise-ის მოდულებზე დამოკიდებულება არ არის |
| 5 | საოპერაციო იზოლაცია: სხვა დოკუმენტებზე ზემოქმედება არ არის | ✔ მოდული სხვა მოდელებს არ ცვლის და არ აფართოებს |
| 6 | იმპორტის შემდეგ Odoo-ს კავშირები ინიშნება გადასახედად | ✔ ტესტი + ინტერფეისი |

### 12. სამომავლო გაფართოებები (არ შედის ამ ვერსიაში)

- ორი ვერსიის ვიზუალური შედარება (diff);
- პროცესების დამტკიცება რამდენიმე საფეხურად (Approvals);
- Visio-ში ექსპორტი (.vsdx);
- KPI-ების მიბმა ამოცანებზე;
- დიაგრამის შინაარსის (სახელები, ინსტრუქციები) მრავალენოვნება;
- ქვეპროცესზე (Call Activity) დაწკაპუნებით შესაბამისი პროცესის გახსნა (ახლა — ბმულით „ბიზნესპროცესი“).
